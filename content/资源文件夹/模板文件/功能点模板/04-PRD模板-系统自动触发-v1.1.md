# PRD模板-系统自动触发-v1.1

### 业务规则

当xxx时，系统自动创建xxx。

触发条件：
xxx

取值逻辑：
【单据编号】自动生成
【字段1】取自xxx
【字段2】按xxx计算
【字段3】为空/默认xxx

### 异常场景

xxx为空：不生成/允许为空/提示xxx
已存在xxx：不生成/提示xxx
存在多个xxx：每个xxx生成一条/只生成一条

### 通过校验后执行

执行1：生成xxx
执行2：关联xxx
执行3：更新xxx
执行4：记录操作日志

### 不在范围内

不支持xxx

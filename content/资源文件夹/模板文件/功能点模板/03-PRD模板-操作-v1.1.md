### 业务规则

入口：从xxx页面点击「xxx」

展示逻辑：xxx

当用户点击「xxx」时，xxx。

基础信息
【单据编号】自动生成/展示
【状态】展示
操作信息
【字段1】文本输入框，默认空
【字段2】数值输入框，默认0，必填
【字段3】单选选择器，默认xxx，必填
【字段4】多选选择器，默认xxx，非必填
【操作时间】日期选择器，默认当天，必填
【附件】附件上传，非必填
【备注】文本输入框，非必填
明细信息
【序号】从1开始，升序自增1
【明细字段1】单选选择器，默认xxx，必填
【明细字段2】多选选择器，默认xxx，非必填

### 点击提交/确定时校验

校验1：前端校验xxx；不满足提示：xxx
校验2：后端校验单据状态是否为xxx；不满足提示：单据状态为{状态}，无法操作
校验3：后端校验当前用户是否有操作权限；不满足提示：无权限操作
校验4：后端校验xxx；不满足提示：xxx

### 通过校验后执行

执行1：生成/更新主表
执行2：生成/更新明细
执行3：将单据状态从xxx-->xxx
执行4：更新/释放/回写关联数据
执行5：记录操作日志

### 异常场景

xxx为空：不允许提交/允许为空/提示xxx
状态不满足：不允许操作，提示xxx
已存在下游数据：不允许操作/允许操作并同步更新xxx
重复点击：只执行一次/提示处理中

### 不在范围内

不支持xxx

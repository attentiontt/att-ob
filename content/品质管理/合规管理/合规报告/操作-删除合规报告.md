# 操作-删除合规报告

### 业务规则

入口：从「合规报告管理」列表点击「删除」。

展示逻辑：弹出确认弹窗，文案为「删除后无法恢复，你还要继续吗？」。

### 点击确认时校验

校验1：后端校验该报告是否绑定任意 Msku；不满足提示：「已绑定msku，无法删除」。
校验2：后端校验当前用户是否有操作权限；不满足提示待确定。

### 通过校验后执行

执行1：真删合规报告。
执行2：记录操作日志。

### 待确定

已上传附件是否同步物理删除待确定。

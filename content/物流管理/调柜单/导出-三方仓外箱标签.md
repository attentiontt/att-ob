# 操作：导出三方仓外箱标签

### 业务规则

入口：从「调柜单列表」或调柜单详情点击「导出三方仓外箱标签」。

展示逻辑：发货标识为「是」时展示按钮；点击后打开头程发货单详情中的「查看」弹窗；头程发货单已核对时展示对应文件，未核对时展示内容为空。

### 点击提交/确定时校验

校验1：原型未展开额外校验，待确定

### 通过校验后执行

执行1：导出三方仓外箱标签文件，文件格式和命名规则待确定
